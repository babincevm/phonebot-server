const {Survey, Test} = require('./Quiz');
const {SurveyGroup, TestGroup} = require('./Group');
const {SurveySubgroup, TestSubgroup} = require('./Subgroup');

module.exports = {
  Survey,
  Test,
  SurveyGroup,
  TestGroup,
  SurveySubgroup,
  TestSubgroup,
};
