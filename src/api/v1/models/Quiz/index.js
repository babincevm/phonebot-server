const {Survey, Test} = require('./Quiz');
const {SurveyGroup, TestGroup} = require('./Group');
const {SurveySubgroup, TestSubgroup, Subgroup} = require('./Subgroup');

module.exports = {
  Survey,
  Test,
  SurveyGroup,
  TestGroup,
  Subgroup,
  SurveySubgroup,
  TestSubgroup,
};
